{
  "opensop": "0.8",
  "name": "incident-postmortem",
  "version": "1.1.0",
  "description": "Collect incident details via form, gate on human sign-off, then emit a structured postmortem Markdown document — or a rejection record if the postmortem is not approved.",
  "inputs": {
    "team": "Platform"
  },
  "steps": [
    {
      "id": "collect-incident",
      "type": "form",
      "description": "Enter the incident details.",
      "inputs": [
        {
          "name": "title",
          "type": "string",
          "required": true,
          "description": "One-line incident title"
        },
        {
          "name": "severity",
          "type": "enum",
          "required": true,
          "values": [
            "SEV1",
            "SEV2",
            "SEV3",
            "SEV4"
          ],
          "description": "Incident severity level"
        },
        {
          "name": "timeline",
          "type": "string",
          "required": true,
          "description": "Chronological timeline of events (detection to resolution)"
        },
        {
          "name": "impact",
          "type": "string",
          "required": true,
          "description": "Customer and business impact"
        },
        {
          "name": "root_cause",
          "type": "string",
          "required": false,
          "description": "Root cause (if known)"
        }
      ]
    },
    {
      "id": "sign-off",
      "type": "approval",
      "description": "Incident commander or engineering lead must approve the postmortem before it is published.",
      "prompt": "Review the collected incident details and approve the postmortem for publication, or reject if it needs revision.",
      "decision_field": "decision"
    },
    {
      "id": "format-postmortem",
      "type": "shell",
      "description": "Emit the postmortem document (approved) or a rejection record (rejected).",
      "run": "set -o pipefail; jq -r '\n  if (.[\"sign-off\"].decision // \"reject\") == \"approve\" then\n    \"# Postmortem: \" + ((.[\"collect-incident\"].title // \"(untitled)\") | tostring) + \"\\n\\n\" +\n    \"**Severity:** \" + ((.[\"collect-incident\"].severity // \"?\") | tostring) + \"\\n\" +\n    \"**Team:** \" + ((.inputs.team // \"Platform\") | tostring) + \"\\n\" +\n    \"**Status:** Approved\\n\\n\" +\n    \"## Timeline\\n\\n\" + ((.[\"collect-incident\"].timeline // \"(none)\") | tostring) + \"\\n\\n\" +\n    \"## Impact\\n\\n\" + ((.[\"collect-incident\"].impact // \"(none)\") | tostring) + \"\\n\\n\" +\n    if ((.[\"collect-incident\"].root_cause // \"\") | tostring) != \"\" then\n      \"## Root Cause\\n\\n\" + ((.[\"collect-incident\"].root_cause // \"\") | tostring) + \"\\n\"\n    else\n      \"## Root Cause\\n\\n_Under investigation._\\n\"\n    end\n  else\n    \"# Postmortem REJECTED — not published\\n\\n\" +\n    \"**Incident:** \" + ((.[\"collect-incident\"].title // \"(untitled)\") | tostring) + \"\\n\" +\n    \"**Severity:** \" + ((.[\"collect-incident\"].severity // \"?\") | tostring) + \"\\n\" +\n    \"**Team:** \" + ((.inputs.team // \"Platform\") | tostring) + \"\\n\" +\n    \"**Status:** Rejected — postmortem requires revision before publication.\\n\"\n  end\n' <<< \"$OSL_CONTEXT\" | jq -Rs '{postmortem: .}'"
    }
  ],
  "sop": {
    "source": "opensop/incident-postmortem",
    "install": "opensop pull opensop/incident-postmortem",
    "tags": [
      "incidents",
      "postmortem",
      "engineering",
      "devops",
      "reliability"
    ]
  }
}
